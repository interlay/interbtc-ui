import styled from 'styled-components';

import { Span } from '../Text';
import { theme } from '../theme';

const StyledSliderThumbWrapper = styled.div``;

export { StyledInput, StyledLabel, StyledSwitch, StyledWrapper };
