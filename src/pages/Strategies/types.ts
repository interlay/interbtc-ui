enum StrategyRisk {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high'
}

enum StrategyType {
  BTC_LOW_RISK = 'btc-low-risk'
}

enum StrategyFormType {
  DEPOSIT,
  WITHDRAW
}

export { StrategyFormType, StrategyRisk, StrategyType };
