// ray test touch <
import { SubstrateProvider } from '@/substrate-lib/substrate-context';

// http://localhost:3000/substrate-context-demo?rpc=wss://rpc.polkadot.io
const SubstrateContextDemo = (): JSX.Element => {
  return <SubstrateProvider>SubstrateContextDemo</SubstrateProvider>;
};

export default SubstrateContextDemo;
// ray test touch >
