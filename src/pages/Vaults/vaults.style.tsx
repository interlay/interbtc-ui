// TODO: this styling is temporary, and will be replaced by the component library
// layout components when KINT support is introduced.
import styled from 'styled-components';

const Vaults = styled.div`
  display: flex;
  justify-content: center;
`;

export { Vaults };
