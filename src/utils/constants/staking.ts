
import {
  VOTE_GOVERNANCE_TOKEN_SYMBOL,
  GOVERNANCE_TOKEN_SYMBOL
} from 'config/relay-chains';

const ESTIMATED_GOVERNANCE_TOKEN_REWARDS_TOOLTIP =
  // eslint-disable-next-line max-len
  `The estimated amount of ${GOVERNANCE_TOKEN_SYMBOL} you will receive as rewards. Depends on your proportion of the total ${VOTE_GOVERNANCE_TOKEN_SYMBOL}.`;

export {
  ESTIMATED_GOVERNANCE_TOKEN_REWARDS_TOOLTIP
};
